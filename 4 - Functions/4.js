// Functions

// Substraction

let resta = function(x, i) {
  return x - i;
};
console.log("substraction result " + resta(69, 15));

// Division

let division = function(x, i) {
  return x / i;
};
console.log("division result " + division(56, 7));

// Multiplication

let multi = function(x, i) {
  return x * i;
};
console.log("multiplication result " + multi(44, 23));

//
